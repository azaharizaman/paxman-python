"""Capabilities subpackage — the ONLY extension point of Paxman (PRD §5.3).

Everything Paxman knows how to canonicalize lives behind one uniform interface.
Each domain is its own package; every package shares the same shape.
"""
