"""Registry subpackage — the frozen roll call of capabilities (PRD §5.4).

Capabilities register before the engine runs; the roster is then frozen for the
life of the process. Turns Resolve from negotiation into a closed lookup.
"""
